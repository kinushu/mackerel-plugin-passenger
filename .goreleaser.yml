builds:
  - binary: mackerel-plugin-passenger
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - 386
      - amd64
      - arm64
archives:
  - format: zip
    name_template: "snapshot/{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
release:
  github:
    owner: kinushu 
    name: mackerel-plugin-passenger
